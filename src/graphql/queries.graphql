mutation AddForm(
    $credits: String!
    $email: String!
    $number: Float!
    $dateOfBirth: Date!
    $message: String
) {
    addForm(
        credits: $credits
        email: $email
        number: $number
        dateOfBirth: $dateOfBirth
        message: $message
    ) {
        status
    }
}

